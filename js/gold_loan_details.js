document.addEventListener("DOMContentLoaded", loadGoldLoanCustomers);
const goldMoney = value => `₹${Number(value || 0).toLocaleString("en-IN", {minimumFractionDigits:2, maximumFractionDigits:2})}`;
async function loadGoldLoanCustomers() { const body = document.getElementById("gold-loan-details-body"); try { const response = await fetch(APPS_SCRIPT_URL + "?t=" + Date.now()), data = await response.json(), loans = data.gold_loans || []; if (!loans.length) { body.innerHTML = '<tr><td colspan="9" style="padding:30px;text-align:center">No Gold Loan customer found.</td></tr>'; return; } body.innerHTML = loans.map(loan => `<tr><td>${loan["Application No"] || loan.ID}</td><td><strong>${loan["Borrower Name"] || "—"}</strong></td><td>${loan["Mobile No"] || "—"}</td><td>${Number(loan["Total Net Weight (g)"] || 0).toFixed(3)} g</td><td>${goldMoney(loan["Eligible Amount"])}</td><td>${goldMoney(loan["Loan Amount Requested"])}</td><td>${loan["Maturity Date"] || "—"}</td><td>${loan.Status || "Active"}</td><td style="white-space:nowrap"><a class="gold-action gold-edit" href="GoldLoanEntry.html?edit=${encodeURIComponent(loan.ID)}"><i class="fa-solid fa-pen"></i></a><button class="gold-action gold-delete" onclick="deleteGoldLoan('${loan.ID}')"><i class="fa-solid fa-trash"></i></button></td></tr>`).join(""); } catch (error) { console.error(error); body.innerHTML = '<tr><td colspan="9" style="padding:30px;text-align:center;color:#c43b2d">Could not load Gold Loan customers.</td></tr>'; } }
async function deleteGoldLoan(id) { if (!confirm("Delete this Gold Loan customer? This cannot be undone.")) return; try { const response = await fetch(APPS_SCRIPT_URL, {method:"POST", body:JSON.stringify({action:"delete_gold_loan", id})}), result = await response.json(); if (result.status !== "success") throw new Error(result.message); loadGoldLoanCustomers(); } catch (error) { alert("Could not delete Gold Loan customer: " + error.message); } }
